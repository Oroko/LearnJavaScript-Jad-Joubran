/**
 * @param {array} elements
 */

 function loopThroughElements(elements){
   elements.forEach(function(element){
     console.log(element)
   })
 }

 console.log(loopThroughElements(['John','Jennifer','Madison']))